# Dockerfile — only exists to demonstrate .dockerignore behaviour
# This does nothing useful — it is just a valid Dockerfile so
# docker build has something to read when we test the build context size

FROM alpine
# Just print a message and exit
CMD ["echo", "optimisation demo"]
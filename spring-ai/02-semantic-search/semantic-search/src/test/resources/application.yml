spring:
  # Tests must never touch the network. Disable auto-pull of Ollama models; the
  # embedding model bean itself is replaced by a mock in the smoke test.
  ai:
    ollama:
      base-url: http://localhost:11434
      init:
        pull-model-strategy: never


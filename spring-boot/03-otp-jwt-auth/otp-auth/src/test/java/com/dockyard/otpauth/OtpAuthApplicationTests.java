package com.dockyard.otpauth;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * OtpAuthApplicationTests — smoke test that the Spring context (security, JPA,
 * JWT beans, config properties) all wire up and start successfully.
 */
@SpringBootTest
class OtpAuthApplicationTests {

    @Test
    void contextLoads() {
        // If the application context fails to start, this test fails.
    }
}

